const array = ['Ford', 'Mazda', 'BMW', 'Audi'];

// const el = array.pop(); // remove and returns last element

// console.log(array);
// console.log(el);

// const el = array.shift(); // remove and returns first element

// console.log(array);
// console.log(el);

// const el = array.unshift('Mercedes'); // put element to first

// console.log(array);
// console.log(el);

// const arrayDeleted = array.splice(1,2, "Subaru", "Kia"); // change the defined elements in the starting array

// console.log(array, arrayDeleted);

// const arrayDeleted = array.splice(1,0, "Subaru", "Kia"); // add elements after the defined start element

// console.log(array, arrayDeleted);

// const filteredArray = array.filter(car => car.toLowerCase().includes("a") || car.toLowerCase().includes("f"));

// console.log(filteredArray);

// const filterCar = (car) => {
//     return car.toLowerCase().includes("a") || car.toLowerCase().includes("f");
// };

// const filteredArrayNew = array.filter(filterCar);

// console.log(filteredArrayNew);

// const newArray = array.concat("Kia", "Subaru");

// console.log(newArray);

// const car = array.find(car => car.startsWith("B");

// console.log(car);

// const car = array.findIndex(car => car.startsWith("B");

// console.log(car);

// console.log(array.join("|")); // join array to string by separator

// console.log(array.join("|").split("|")); // split string to array by separator

// const newArray = array.slice(1,3); 

// console.log(newArray);